# M01 plan (stub)

**Status:** Planned — do not treat as active until M00 is closed.

## M01 priority

End-to-end verification through **Kaggle Community Benchmarks**.

## Target

Prove that LUCID’s local benchmark structure can be expressed through Kaggle’s task/benchmark workflow, run against the platform correctly, and be measured **without changing** core benchmark semantics (episodes, typed outputs, scoring profile v1.1.0).

## Out of scope for M01 (initial)

- New template families beyond transport needs
- Changing scalar weights or profile semantics (would be a version bump)
